#include<stdio.h>
int main(){
    float radius, area;
    float pi=3.14;
     printf("Enter the value of radius: \n"); 
     scanf("%f", &radius);
     area = pi*radius;
      printf("The area of circle is %f", area); 
    return 0;
}